{
  "name": "vanilla-cart-ts",
  "version": "1.0.0",
  "private": true,
  "description": "Adaptive catalog (~12 cards) + cart. Vanilla JS deliverable; TypeScript for development only.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "python -m http.server 5173 -d public",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.6.3"
  }
}
